node_modules
dist
dist-ssr
.vite
*.local
.DS_Store
.env
.env.*
!.env.example

# Lokale Testausgaben (Beispiel-PDF/-DOCX aus scripts/)
/beispiel/

# Briefvorlagen und Logos: Firmenmaterial gehört nicht in ein öffentliches
# Repository. Die Vorlage wird in der App selbst hinterlegt, nicht mitgeliefert.
/vorlage/

# Lokale Claude-Code-Einstellungen (geräteabhängig)
.claude/settings.local.json
